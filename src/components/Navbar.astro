<header class="header">
  <img src="/logo.png" alt="logo" />

  <input type="checkbox" id="check" />
  <label for="check" class="icons">
    <i class="bx bx-menu" id="menu-icon"></i>
    <i class="bx bx-x" id="close-icon"></i>
  </label>

  <nav class="navbar">
    <a style="--i:0;" href="#home">Home</a>
    <a style="--i:1;" href="#about">About Us</a>
    <a style="--i:2;" href="#ourmiss">Our Mission</a>
    <a style="--i:3;" href="#ourpor">Our Programs</a>
    <a style="--i:4;" href="#getinv">Get Involved</a>
    <a style="--i:5;" href="#don">Donation</a>
    <a style="--i:6;" href="#evenew">Event And News</a>
    <a style="--i:7;" href="#con">Contact</a>
  </nav>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    padding: 10px 10%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    z-index: 100;
  }

  .header::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(5px);
    z-index: -1;
  }

  .header img {
    width: 70px;
    height: 70px;
  }

  #check {
    display: none;
  }

  .icons {
    right: 5%;
    position: absolute;
    font-size: 3rem;
    color: #fff;
    cursor: pointer;
    display: none;
  }

  .navbar a {
    font-size: 1.2rem;
    color: #fff;
    text-decoration: none;
    font-weight: 500;
    margin-left: 2.5rem;
  }

  .navbar a:hover {
    color: #005eb6;
    transition: 0.3s ease;
  }

  @media (max-width: 1600px) {
    .header {
      padding: 7px 10%;
    }

    .header img {
      width: 80px;
      height: 80px;
    }

    .navbar a {
      font-size: 1.1rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 1rem;
    }
  }
  @media (max-width: 1367px) {
    .header {
      padding: 7px 10%;
    }

    .header img {
      width: 70px;
      height: 70px;
    }

    .navbar a {
      font-size: 1.1rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 1rem;
    }
  }

  @media (max-width: 1280px) {
    .header {
      padding: 7px 10%;
    }

    .header img {
      width: 70px;
      height: 70px;
    }

    .navbar a {
      font-size: 1rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 1rem;
    }
  }

  @media (max-width: 1200px) {
    .header {
      padding: 5px 5%;
    }

    .header img {
      width: 60px;
      height: 60px;
    }

    .navbar a {
      font-size: 0.9rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 1rem;
    }
  }

  @media (max-width: 992px) {
    .header {
      padding: 5px 5%;
    }

    .header img {
      width: 50px;
      height: 50px;
    }

    .navbar a {
      font-size: 0.8rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 1rem;
    }
  }

  @media (max-width: 920px) {
    .header {
      padding: 5px 5%;
    }

    .header img {
      width: 50px;
      height: 50px;
    }

    .navbar a {
      font-size: 0.75rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 1rem;
    }
  }

  @media (max-width: 860px) {
    .header {
      padding: 5px 5%;
    }

    .header img {
      width: 50px;
      height: 50px;
    }

    .navbar a {
      font-size: 0.7rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 1rem;
    }
  }

  @media (max-width: 810px) {
    .header {
      padding: 5px 3%;
    }

    .header img {
      width: 50px;
      height: 50px;
    }

    .navbar a {
      font-size: 0.75rem;
      color: #fff;
      text-decoration: none;
      font-weight: 500;
      margin-left: 0.7rem;
    }
  }

  @media (max-width: 768px) {
    .header img {
      width: 50px;
      height: 50px;
    }

    .icons {
      display: inline-flex;
    }

    #check:checked ~ .icons #menu-icon {
      display: none;
    }

    .icons #close-icon {
      display: none;
    }

    #check:checked ~ .icons #close-icon {
      display: block;
    }

    .navbar {
      position: absolute;
      top: 100%;
      left: 0;
      width: 100%;
      height: 0;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(5px);
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
      overflow: hidden;
      transition: 0.3s ease;
    }

    #check:checked ~ .navbar {
      height: 27rem;
    }

    .navbar a {
      display: block;
      font-size: 1.1rem;
      margin: 1.5rem 0;
      text-align: center;
      transform: translateY(-50px);
      opacity: 0;
      transition: 0.3s ease;
    }

    #check:checked ~ .navbar a {
      transform: translateY(0);
      opacity: 1;
      transition-delay: calc(0.1s * var(--i));
    }
  }
</style>
